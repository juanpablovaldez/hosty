name: Deploy Backend to DEV

on:
  push:
    branches: [dev]
    paths:
      - "backend/**"
      - "package.json"
      - "pnpm-lock.yaml"

jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest

    steps:
      - name: Get runner public IP
        id: ip
        run: echo "ip=$(curl -s https://checkip.amazonaws.com)/32" >> $GITHUB_OUTPUT

      - name: Allow runner IP in security group
        run: |
          aws ec2 authorize-security-group-ingress \
            --group-id ${{ secrets.EC2_SG_ID_DEV }} \
            --protocol tcp --port 22 \
            --cidr ${{ steps.ip.outputs.ip }}
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: us-east-1

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST_DEV }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY_DEV }}
          envs: GH_TOKEN
          script: |
            export PATH=$PATH:/usr/bin:/usr/local/bin

            # Bootstrap: install Node.js if not present
            if ! command -v node &> /dev/null; then
              curl -fsSL https://deb.nodesource.com/setup_22.x | sudo bash -
              sudo apt-get install -y nodejs
            fi

            # Bootstrap: install pnpm if not present
            if ! command -v pnpm &> /dev/null; then
              sudo npm install -g pnpm
            fi

            # Bootstrap: install pm2 if not present
            if ! command -v pm2 &> /dev/null; then
              sudo npm install -g pm2
              sudo env PATH=$PATH /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u ubuntu --hp /home/ubuntu || true
              sudo systemctl enable pm2-ubuntu || true
            fi

            set -e

            # First deploy: clone the repo
            if [ ! -d ~/hosty ]; then
              git clone https://x-access-token:${GH_TOKEN}@github.com/juanpablovaldez/hosty.git ~/hosty
            fi

            cd ~/hosty

            # Pull latest changes
            git fetch origin dev
            git reset --hard origin/dev

            # Install dependencies
            pnpm install --frozen-lockfile

            # Apply database migrations
            pnpm --filter backend exec prisma migrate deploy

            # Build backend
            pnpm --filter backend build

            # Start or restart the service
            pm2 restart backend --update-env \
              || pm2 start "node backend/dist/src/main.js" --name backend
            pm2 save
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Remove runner IP from security group
        if: always()
        run: |
          aws ec2 revoke-security-group-ingress \
            --group-id ${{ secrets.EC2_SG_ID_DEV }} \
            --protocol tcp --port 22 \
            --cidr ${{ steps.ip.outputs.ip }}
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: us-east-1
