# Skill Registry — hosty

Generated by `sdd-init`. Mode-independent infrastructure file (kept regardless of SDD artifact store).

## Project Convention Source

- `/Users/vleonardojuanpablo/Documents/workspace/code/hosty/CLAUDE.md` — authoritative project
  conventions. Key rules any sub-agent touching this repo must follow:
  - Frontend-only active codebase. `backend/` folder is **deleted from disk** (see sdd-init
    project context) — never add NestJS code or axios calls in `frontend/`.
  - React 19 + Vite + TS strict, Tailwind v4 (CSS-first, no `tailwind.config`), shadcn/ui,
    TanStack Router/Query/Form, Zustand, react-i18next, lucide-react (`strokeWidth={1.5}` always).
  - Supabase client: `frontend/src/shared/lib/supabase.ts`, types in `database.types.ts`.
  - ESLint 9 strict (`@typescript-eslint/no-explicit-any` = error), Prettier (single quotes,
    trailing commas — though no `.prettierrc` file actually exists, see risk below).
  - Folder convention: `features/<name>/{components,api,types.ts}`, `components/{layout,ui}`,
    `routes/` (file-based), `shared/{lib,store}`, `i18n/`.
  - No comments unless the WHY is non-obvious.

## Compact Rules (inject into sub-agent prompts touching `frontend/`)

```
## Project Standards (auto-resolved)
- Hosty = React 19 + Vite + TS strict + Supabase (BaaS). NO NestJS/axios in frontend/ — backend/ is deleted.
- Brand tokens only (bg-primary, text-foreground, etc.) — never raw hex in JSX.
- lucide-react icons only, always strokeWidth={1.5}.
- Folder convention: features/<name>/{components,api,types.ts}.
- No comments unless WHY is non-obvious; self-documenting names.
- ESLint strict: no `any`. Tailwind v4 is CSS-first (@theme block in src/index.css) — no tailwind.config file.
```

## Skills Scanned

No project-level `.claude/skills/` directory exists in this repo. User-level skills at
`~/.claude/skills/` were scanned for triggers relevant to this project's stack (React/Supabase/
docs, not Next.js/Vercel) — none apply to routine frontend or documentation work beyond the
general-purpose ones below.

| Skill | Trigger relevance to hosty |
|---|---|
| `frontend-design` / `frontend-design-review` | Only if asked to build/review new UI components |
| `web-design-guidelines` | Only for UI/accessibility review requests |
| `security-review` | Only when touching auth, RLS, or secrets |
| `sdd-*` (this family) | Active — used for this and future structured changes |

No project-specific skill registry existed before this run.

## Risks / Discrepancies Noted During Detection

- `prettier` is referenced by npm scripts (`format`, `format:check`) but is **not** installed as
  a devDependency and has **no config file** — CLAUDE.md's "single quotes, trailing commas" rule
  is currently unenforceable by tooling.
- `backend/` is documented in CLAUDE.md as "retained but inactive" — it is actually **deleted**
  from the working tree (commit `3a89616`). Sub-agents should not expect to find it.
